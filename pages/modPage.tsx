import UpgradeModal from '../components/upgradeModal'
import PoolModal from '../components/poolModal'

const ModPage = () => {
    return (
        // <UpgradeModal NFTlevel={2} />
        <PoolModal elements={[]}/>
        );
}

export default ModPage;